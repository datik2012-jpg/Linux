////////////Users and Groups //////////

>sudo adduser dani - add user.As well new group dani will be created. will bec created home directpry /home/dani.Default shell will be set to  /bin/bash and the files from /etc/skel wil be copied to /home/dani when dani login 
to check which files inside
>ls -a /etc/skel   - skeleton directory (later explain)

bob@ubuntu-host ~ ➜  ls -a /etc/skel
.  ..  .bash_logout  .bashrc  .profile

>sudo passwd dani - set the passwrod for user

>sudo deluser dani - delete account (will autodelete as well a group dani).but /home/dani - directory will remain

>sudo deluser --remove-home dani  - delete as well home default directory (/home/dani)

>sudo adduser -shell /bin/othershell --home /home/otherdirectory/ dani - create user in different not default directory

>sudo adduser --shell /bin/othershell dani  -  when we want to keep default home directory /home/dani but shell option is different 
The shell you specify must exist and be listed in /etc/shells.
If it’s not, login may fail.

for example:
/bin/bash
/bin/sh
/bin/zsh
/usr/bin/fish

When we want to add new shell:
Install Zsh: sudo apt install zsh
Install Fish: sudo apt install fish
Make sure it’s executable:sudo chmod +x /usr/local/bin/myshell
Step 2 — Add the shell to /etc/shells
sudo nano /etc/shells
/usr/bin/zsh
/usr/bin/fish
/usr/local/bin/myshell
Step 3 — Use the shell for a user
sudo adduser --shell /usr/bin/zsh dani
Or change an existing user’s shell:sudo chsh -s /usr/bin/zsh dani

How to verify the shell works: ls -l /usr/bin/zsh
Check that it’s in /etc/shells:cat /etc/shells
If both are correct, the user can log in normally.


Account details :username,user IDs,groups ,prefered shell and home directories are storied at /etc/passwd

cat /etc/passwd

dani:x:1001:1001::/home/dani/:/bin/shell
1001 - is ID number associated with dani's username
1001 - is ID of primary group - dani's group in this case


>sudo adduser --uid 1100 dani - manually add different user ID during creation

>ls -l /home/ -  to see which users or groups owns files or directories

>ls -ln /home/ -  to see as well users and groups IDs


>id - get IDs for the current user

>whoami



>sudo adduser --system --no-create-home sysacc - create system account  - will not creat home directory .ID for kind of these accounts id<1000.Use for services(daemons)

>adduser --help - can give more instractions in case we forget


>sudo usermod  --home /home/otherdirectory --move-home dani  - to change user directory to otherdirectory

>sudo usermode -d /home/otherdirectory -m dani  - move direcotry will all access

>sudo usermod --login dani vasya   or sudo usermod -l dani vasya  - change user name

>sudo usermod --shell /bin/othershell dani  OR sudo usermod -s /bin/newshell vasya

>sudo usermod --lock dani - disable without deleting the user.But user will be able to login with SSH key !!!

>sudo usermod --unlock dani 

>sudo usermod --expiredate 2026-10-10 dani OR sudo usermod -e 2026-10-10 dani - after expiration date user will not able to login

>sudo usermod --expiredate "" dani - to remove expiration date from user


>sudo chage --lastday 0 dani - expire the password (user will need to change it) OR sudo chage -d 0 dani 

>sudo chage --lastday -1 dani - to cancel epire the passwrod OR sudo chage -d -1 dani

>sudo chage --maxdays 30 dani OR sudo chage -M 30 dani -  make user change his password every 30 days

>sudo chage --maxdays -1 dani OR sudo chage -M -1 dani - password never expires

>sudo chage --list jane OR sudo chage -l dani   - to check when account expires

/////////////Groups /////////////////

>sudo adduser dani
>sudo groupadd developers

>sudo gpasswd --add dani developers - will add dani to developers group
or
>sudo gpasswd --a dani developers

>groups dani  - to check to which groups belongs user 

dani: dani developers - first is primary group (called dani) secondary is developes

>sudo gpasswd --delete dani developers - delete the user from the group
>sudo gpasswd -d dani developers 

Sometimes we want to change PRIMARY user group:
>sudo usermode -g developers dani  - do not confuse with (-G) which will change secondary group
to avoid a mistake use: sudo usermod --gid developers dani  - for promary change group

>groups dani
dani:developers   - as you can see primary group been changed

>gpasswd --help - to refresh the info about the commnand options

>sudo groupmod --new-name programmers developers  - rename group to programmers OR sudo groupmode -n programmers developers

>sudo groupdel programmers - to delete the group (if gives error for specific user which has promary group: sudo usermod --gid dani dani - change the primary group for this user with error)

/////////////////LAB //////////


Set the jane user account to expire on March 1, 2030.

bob@ubuntu-host ~ ✖ sudo usermod -e 2030-03-01 jane

Create a system account called apachedev

sudo adduser --system --no-create-home apachedev

bob@ubuntu-host ~ ✖ sudo adduser --system --no-create-home apachedev
Adding system user `apachedev' (UID 107) ...
Adding new user `apachedev' (UID 107) with group `nogroup' ...
Not creating home directory `/home/apachedev'.

Jane's account, i.e., jane, is expired.Unexpire the same and make sure it never expires again.

bob@ubuntu-host ~ ✖ sudo usermod -e "" jane


bob@ubuntu-host ~ ✖ sudo chage -M -1 jane

bob@ubuntu-host ~ ✖ sudo chage -l jane
Last password change                                    : Apr 19, 2026
Password expires                                        : never
Password inactive                                       : never
Account expires                                         : never
Minimum number of days between password change          : 0
Maximum number of days between password change          : -1
Number of days of warning before password expires       : 7


Create a user account called jack with home directory and set its default login shell to be /bin/csh.


sudo adduser --shell /bin/csh jack

bob@ubuntu-host ~ ✖ sudo adduser --shell /bin/csh jack
Adding user `jack' ...
Adding new group `jack' (1002) ...
Adding new user `jack' (1002) with group `jack' ...
Creating home directory `/home/jack' ...
Copying files from `/etc/skel' ...
New password: 
Retype new password: 
passwd: password updated successfully
Changing the user information for jack
Enter the new value, or press ENTER for the default
        Full Name []: jack
        Room Number []: 100
        Work Phone []: 1010101010
        Home Phone []: 1010011001
        Other []: 10
Is the information correct? [Y/n] Y



Delete the user account called jack and ensure his home directory is removed.


bob@ubuntu-host ~ ✖ sudo deluser --remove-home jack   - will remove the user and it's home directory
Looking for files to backup/remove ...
Removing files ...
Removing user `jack' ...
Warning: group `jack' has no more members.
Done.


Mark the password for jane as expired to force her to immediately change it the next time she logs in.

>sudo chage --lastday 0 jane - expire the password (user will need to change it) OR sudo chage -d 0 jane


Add the user jane to the group called developers.



>sudo gpasswd --add jane developers - will add dani to developers group
or
>sudo gpasswd --a jane developers

bob@ubuntu-host ~ ➜  sudo gpasswd --add jane developers
Adding user jane to group developers

Create a group named cricket and set its GID to 9875

bob@ubuntu-host ~ ✖ sudo groupadd cricket


View all groups (simple list)
getent group  OR cat /etc/group
cricket:x:1003:

>sudo groupmod -g 9875 cricket  - change group ID

Validation:getent group

cricket:x:9875:

You already created a group cricket in the previous question. Now, rename this group soccer while preserving the same GID.


sudo groupmod --new-name soccer cricket

VAlidation: bob@ubuntu-host ~ ➜  cat /etc/group
soccer:x:9875:


Create a user sam with UID 5322. Also, make it a member of the soccer group.

I check before the soccer group ID and use it with help ,to see the options to run with ID for user and ID for a group (adduser --help)

bob@ubuntu-host ~ ➜  sudo adduser sam --uid 5322 --gid 9875
Adding user `sam' ...
Adding new user `sam' (5322) with group `soccer' ...
Creating home directory `/home/sam' ...
Copying files from `/etc/skel' ...
New password: 
Retype new password: 
passwd: password updated successfully
Changing the user information for sam
Enter the new value, or press ENTER for the default
        Full Name []: rer
        Room Number []: erre
        Work Phone []: er
        Home Phone []: er
        Other []: er
Is the information correct? [Y/n] Y

Validation:

bob@ubuntu-host ~ ✖ id sam
uid=5322(sam) gid=9875(soccer) groups=9875(soccer)

The issue primary group as well soccer and need chaneg to sam

bob@ubuntu-host ~ ✖ sudo groupadd sam

sudo usermod -g sam sam

sudo usermod -aG soccer sam


bob@ubuntu-host ~ ➜  id sam
uid=5322(sam) gid=9876(sam) groups=9876(sam),9875(soccer)

Update primary group of user sam and set it to rugby

bob@ubuntu-host ~ ➜  id sam
uid=5322(sam) gid=9876(sam) groups=9876(sam),9875(soccer)

bob@ubuntu-host ~ ➜  sudo usermod -g  rugby sam

bob@ubuntu-host ~ ➜  id sam
uid=5322(sam) gid=9877(rugby) groups=9877(rugby),9875(soccer)


Delete the group called appdevs

bob@ubuntu-host ~ ✖ sudo groupdel appdevs

validation:
bob@ubuntu-host ~ ➜  cat /etc/group


Make sure the user jane gets a warning at least 2 days before the password expires.

Validation: sudo chage -W 2 jane

sudo chage -l jane
bob@ubuntu-host ~ ➜  sudo chage -l jane
Last password change                                    : password must be changed
Password expires                                        : password must be changed
Password inactive                                       : password must be changed
Account expires                                         : never
Minimum number of days between password change          : 0
Maximum number of days between password change          : -1
Number of days of warning before password expires       : 2


////////////////////Manage system wide Environment Profiles ///////////

>printenv OR env  - will show env path and variables

>HISTSIZE=1000 to change the max size of shell command history

>history - can show last run commands and HISTSIZE can show (as we set only 1000 last commands)

>echo $HOME will show the home path

>touch $HOME/saved_file - can be used in the scripts depends on the user login /home/dani or /home/alex

To add MY or user own env variables:

>touch .bashrc 
(cat .bashrc to check the content)

To add env variable as global for the entire system:

>sudo vim /etc/environment 
KODEKNLOUD=https://kodekloud.com 

check the variable with even different user:

>echo $KODEKLOUD

Script in this location will auto executed when user LOGIN:

>sudo vim /etc/profile.d/lastlogin.sh  - create a script in this location.Do not add #!/bin/bash whe we create sripts in this dir
echo "Last login" > $HOME/lastlogin
date >> $HOME/lastlogin


Log out and Login and check:
>cat lastlogin

//////////////////////////////Manage Template User Env /////////

When user been created all files from /etc/skel been copied to user new directory .And this is helpful when we want to create a TEMPLATE for user environment

bob@ubuntu-host ~ ➜  ls -a /etc/skel
.  ..  .bash_logout  .bashrc  .profile


We can create a file which will inform new created user about something major or needed or RULES about the server and etc:

>sudo vim /etc/skel/README

Please do not run CPU-intensive processes between 16:00 - 20:00 PM

>sudo adduser dani 

>suod ls -a /home/dani   - (-a) will show the files with . (dot)
.  ..  .bash_logout  .bashrc  .profile READMY - now the file is here

//////////////////////LAB //////////////////////////////////


How can we print the value of an environment variable?

echo $NYVAR

How can we print the value of an environment variable?


bob@ubuntu-host ~ ➜  echo $HOME
/home/bob

Which of the following files can be used to set the globally available environment variables in a Linux-based system?

bob@ubuntu-host ~ ✖ ls /etc/environment
/etc/environment

Print our current user's (bob) environment and save the output in the /home/bob/env file.


You can use the redirection to save your command's output in a file: [your-command] > /home/bob/env

bob@ubuntu-host ~ ➜  printenv
SHELL=/bin/bash
GLOBALENV=OFF
PWD=/home/bob
LOGNAME=bob
XDG_SESSION_TYPE=tty
HOME=/home/bob
LANG=en_US.UTF-8
LS_COLORS=rs=0:di=01;34:ln=01;36:mh=00:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:mi=00:su=37;41:sg=30;43:ca=30;41:tw=30;42:ow=34;42:st=37;44:ex=01;32:*.tar=01;31:*.tgz=01;31:*.arc=01;31:*.arj=01;31:*.taz=01;31:*.lha=01;31:*.lz4=01;31:*.lzh=01;31:*.lzma=01;31:*.tlz=01;31:*.txz=01;31:*.tzo=01;31:*.t7z=01;31:*.zip=01;31:*.z=01;31:*.dz=01;31:*.gz=01;31:*.lrz=01;31:*.lz=01;31:*.lzo=01;31:*.xz=01;31:*.zst=01;31:*.tzst=01;31:*.bz2=01;31:*.bz=01;31:*.tbz=01;31:*.tbz2=01;31:*.tz=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.war=01;31:*.ear=01;31:*.sar=01;31:*.rar=01;31:*.alz=01;31:*.ace=01;31:*.zoo=01;31:*.cpio=01;31:*.7z=01;31:*.rz=01;31:*.cab=01;31:*.wim=01;31:*.swm=01;31:*.dwm=01;31:*.esd=01;31:*.jpg=01;35:*.jpeg=01;35:*.mjpg=01;35:*.mjpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.svg=01;35:*.svgz=01;35:*.mng=01;35:*.pcx=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.m2v=01;35:*.mkv=01;35:*.webm=01;35:*.webp=01;35:*.ogm=01;35:*.mp4=01;35:*.m4v=01;35:*.mp4v=01;35:*.vob=01;35:*.qt=01;35:*.nuv=01;35:*.wmv=01;35:*.asf=01;35:*.rm=01;35:*.rmvb=01;35:*.flc=01;35:*.avi=01;35:*.fli=01;35:*.flv=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.yuv=01;35:*.cgm=01;35:*.emf=01;35:*.ogv=01;35:*.ogx=01;35:*.aac=00;36:*.au=00;36:*.flac=00;36:*.m4a=00;36:*.mid=00;36:*.midi=00;36:*.mka=00;36:*.mp3=00;36:*.mpc=00;36:*.ogg=00;36:*.ra=00;36:*.wav=00;36:*.oga=00;36:*.opus=00;36:*.spx=00;36:*.xspf=00;36:
STARSHIP_SHELL=bash
STARSHIP_SESSION_KEY=2345321269550147
XDG_SESSION_CLASS=user
TERM=xterm-256color
USER=bob
SHLVL=1
XDG_SESSION_ID=c1
XDG_RUNTIME_DIR=/run/user/1000
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus
MAIL=/var/mail/bob
_=/usr/bin/printenv

bob@ubuntu-host ~ ➜  printenv > /home/bob/env


Add an environment variable for user bob.


The variable name should be MYVAR and its value should be TRUE

Add to the current USER !!!!

vim ~/.bashrc

export MYVAR=TRUE

reload:
source ~/.bashrc

ob@ubuntu-host ~ ➜  env
SHELL=/bin/bash
MYVAR=TRUE
GLOBALENV=OFF
PWD=/home/bob

Identify the value of GLOBALENV environment variable and save it in the /home/bob/globalenv file.


You can use the redirection to save your command's output in a file: [your-command] > /home/bob/globalenv

bob@ubuntu-host ~ ➜  env | grep "GLOBALENV"
GLOBALENV=OFF

bob@ubuntu-host ~ ➜  env | grep "GLOBALENV" > /home/bob/globalenv



Whenever we add a new user to the system, some files are copied from a template directory to the user's home directory.


Manually copy those files into the /home/bob/default_data directory.

Created new user:

bob@ubuntu-host ~ ✖ sudo adduser dani
Adding user `dani' ...
Adding new group `dani' (1001) ...
Adding new user `dani' (1001) with group `dani' ...
Creating home directory `/home/dani' ...
Copying files from `/etc/skel' ...
New password: 
Retype new password: 
passwd: password updated successfully
Changing the user information for dani
Enter the new value, or press ENTER for the default
        Full Name []: d
        Room Number []: d
        Work Phone []: d
        Home Phone []: d
        Other []: d
Is the information correct? [Y/n] Y

Now we can see these hidden files in new user dir:

bob@ubuntu-host ~ ➜  sudo ls -a /home/dani
.  ..  .bash_logout  .bashrc  .profile



bob@ubuntu-host ~ ➜  sudo cp -a /home/dani/. /home/bob/default_data/

Correct solution was : sudo cp /etc/skel/.bash* /home/bob/default_data/  - so they were meaning to copy from the skel source manually to the user


Modify the system-wide environment file and make sure that the variable GLOBALOPTION is set to this value: ON. Otherwise, after you modify that file, any user that logs in and types…

bob@ubuntu-host ~ ➜  env
SHELL=/bin/bash
MYVAR=TRUE
GLOBALENV=OFF
PWD=/home/bob

bob@ubuntu-host ~ ✖ sudo vim /etc/environment
GLOBALENV=ON

If you used /etc/environment, you need a full logout/login or:
su - bob

bob@ubuntu-host ~ ➜  sudo nano /etc/environment

bob@ubuntu-host ~ ➜  cat  /etc/environment
PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin"
LANG=en_US.UTF-8
GLOBALENV=ON
MYVAR=TRUE
GLOBALOPTION=ON





Make sure that this command gets executed for any user that logs in to the system:


echo Welcome to our server!


sudo vim /etc/profile.d/global-command.sh
and add there echo "Welcome to our server!"
sudo chmod +x /etc/profile.d/global-command.sh


Make sure that every time a new user account is added to the system, a file called README is copied to the new user's home directory.

bob@ubuntu-host ~ ➜  sudo vim /etc/skel/README


Set a variable named LFCS with value Welcome to the KodeKloud LFCS Labs! for every user that logs into this system.


Note: You may need to login again to make the changes take effect, so run sudo su - <current-user-name>.

bob@ubuntu-host ~ ✖ env
SHELL=/bin/bash
MYVAR=TRUE
GLOBALENV=OFF

sudo vim /etc/environment

LFCS="Welcome to the KodeKloud LFCS Labs!"

Note: You may need to login again to make the changes take effect, so run sudo su - <current-user-name>.


Add the value of $PATH variable for user bob to include $HOME/.config/bin location in the path.

bob@ubuntu-host ~ ➜  ls -a
.  ..  .bash_logout  .bash_profile  .bashrc  .cache  .config  default_data  env  globalenv  .profile  .viminfo

bob@ubuntu-host ~ ➜  vim .bashrc

# Add custom bin directory to PATH
if [ -d "$HOME/.config/bin" ]; then
    PATH="$HOME/.config/bin:$PATH"
fi


///////////////////////////////////////Configure User Resouce Limits ////////////


controls per user and per group resource limits on Linux, such as maximum open files, max processes, memory lock size, and more. These limits apply when users log in through PAM (Pluggable Authentication Modules).

>sudo vim /etc/security/limits.conf 

domain type item value

dani   hard  nproc  10
@developers  soft  nproc 20
*            soft    cpu  5      - * says for everyuser in the system set the limit cpu to 5

>man limits.com  - for more information

If we set limit for user dani:
dani  -  nproc 3     - which says user allowed to run only 3 processes

After modification in the file run log of and log in > sudo -iu dani
>ps | less 
3 processess we will secondary

Try to lounch more processes:
>ls -a | grep bash | less 
Output: Resource temporary unavailable

>logout



To check the limit for the current session:
>ulimit -a

Use can only lower his limits not raise them up:
>ulimit -u 5000   - set less user processes (was 14722 before),More over use can raise only once even of hard is allowed more



>groups - You get the groups for your current user.
dani : dani sudo docker audio

to grand to the user sudo priviligies - need to add him to the sudo group
>sudo grasswd -a alex sudo - now user can do whatever he want in the system

For more fine tuned premissions we have different approach :

first remove alex from the sudo group:
>suod gpasswd -d alex sudo

need to edit the file:
>sudo visudo
%sudo   ALL=(ALL:ALL) ALL

%sudo - users group - define this policy for 
ALL=  - is a host ,can be defined the rule only for the servers with specific IP values
(ALL:  - run as user field,we can type the list of usernames ,by default sudo runs by root user,But sudo can be used DANI can run commands for some others regular user like ALEX(or list of users).So root user can be run as DANI only (can be a list)
ALL) - run as group filed ,so the same here,users that belongs to the sudo group can use sudo to run commands as any group

ALL - list of commands which are listed -and onloy can be running by sudo

Examples define a policy to DANI user to run any commands with sudo:

dani ALL=(ALL)   ALL    -where ALL=(ALL) - we do not specify any group so dani can run commads as any group too

%developers ALL=(ALL) ALL - all group users from developers can runn all commands for all users (nothing defined for groups -so they can run as well for all groups)


Set dani to run sudo commands as user Sasha or Vasya

dani ALL=(sasha,vasya) ALL


dani ALL=ALL  - as well valid format

dani ALL=(ALL) /bin/ls, /bin/stat   - allow to run only those commands

When we login first and want to run sudo it will be asking for a password (to disable it):

%sudo ALL=(ALL) NOPASSWD: ALL 

dani ALL= NOPASSWD:ALL - user able to run sudo commands without providing a password 

Simulate akex user and chech if he has an access to my dir:

>sudo -u alex ls /home/dani 

Note: You are telling the system:

“Run the command ls /home/dani as if you were user alex.”

So instead of you listing /home/dani, the system switches identity to alex and checks whether alex has permission to see inside /home/dani.

Simulate another user without switching shells
No need to run su - alex; just run a single command as alex.

sudo -u alex whoami - just check

Open a shell as alex:
sudo -u alex -s